<UserControl x:Class="SyncOps.Laundry.DesktopUI.Views.Pages.TallerKanbanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ENCABEZADO Y BUSCADOR RÁPIDO DE TICKETS / CÓDIGOS DE BARRAS -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Tablero de Taller &amp; Flujo de Prendas" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                <TextBlock Text="Seguimiento visual del estado de lavado, planchado, arreglos de sastrería y entregas" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- Barra de búsqueda / Lector de código de barras -->
                <Border Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="8,4" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconSearch}" Fill="{StaticResource TextMutedBrush}" Width="14" Height="14" Stretch="Uniform" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBox Width="220" BorderThickness="0" Background="Transparent" Text="{Binding FiltroBusqueda, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" FontSize="13">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding BuscarPorCodigoBarrasCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource PrimaryButton}" Command="{Binding BuscarPorCodigoBarrasCommand}" Content="Buscar Ticket" Margin="0,0,8,0" Padding="12,6"/>
                <Button Style="{StaticResource OutlineButton}" Command="{Binding CargarKanbanCommand}" Content="⟳ Refrescar" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- TABLERO KANBAN DE 4 COLUMNAS PRINCIPALES -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- COLUMNA 1: RECIBIDO / POR INICIAR -->
            <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Cabecera -->
                    <Border Grid.Row="0" Background="{StaticResource StatusTodoBg}" CornerRadius="6" Padding="8,6" Margin="0,0,0,10">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource StatusTodoDot}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="1. RECIBIDO" FontWeight="Bold" FontSize="12" Foreground="{StaticResource StatusTodoText}"/>
                            </StackPanel>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding ColumnaRecibido.Count}" FontWeight="ExtraBold" Foreground="{StaticResource StatusTodoText}" FontFamily="{StaticResource MonoFont}"/>
                        </Grid>
                    </Border>
                    <!-- Tarjetas de órdenes -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ColumnaRecibido}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8" Padding="12">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <TextBlock Text="{Binding NumeroTicket}" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock HorizontalAlignment="Right" Text="{Binding Items.Count, StringFormat='{}{0} pzs'}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" FontSize="12"/>
                                            <TextBlock Text="{Binding FechaPromesaEntrega, StringFormat='Entrega: {0:dd/MM hh:mm tt}'}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,8"/>
                                            
                                            <Button Style="{StaticResource OutlineButton}" Content="Iniciar Proceso ➔" Command="{Binding DataContext.AvanzarEstadoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="6,4" FontSize="11" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- COLUMNA 2: EN LAVADO / SASTRERÍA -->
            <Border Grid.Column="2" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" CornerRadius="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Cabecera -->
                    <Border Grid.Row="0" Background="{StaticResource StatusProgressBg}" CornerRadius="6" Padding="8,6" Margin="0,0,0,10">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource StatusProgressDot}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="2. EN LAVADO / TALLER" FontWeight="Bold" FontSize="12" Foreground="{StaticResource StatusProgressText}"/>
                            </StackPanel>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding ColumnaEnProceso.Count}" FontWeight="ExtraBold" Foreground="{StaticResource StatusProgressText}" FontFamily="{StaticResource MonoFont}"/>
                        </Grid>
                    </Border>
                    <!-- Tarjetas de órdenes -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ColumnaEnProceso}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8" Padding="12">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <TextBlock Text="{Binding NumeroTicket}" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock HorizontalAlignment="Right" Text="{Binding Items.Count, StringFormat='{}{0} pzs'}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" FontSize="12"/>
                                            <TextBlock Text="{Binding FechaPromesaEntrega, StringFormat='Entrega: {0:dd/MM hh:mm tt}'}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,8"/>

                                            <Button Style="{StaticResource PrimaryButton}" Content="Pasar a Planchado ➔" Command="{Binding DataContext.AvanzarEstadoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="6,4" FontSize="11" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- COLUMNA 3: EN PLANCHADO / CONTROL CALIDAD -->
            <Border Grid.Column="4" Background="#FFFBEB" BorderBrush="#FDE68A" BorderThickness="1" CornerRadius="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Cabecera -->
                    <Border Grid.Row="0" Background="{StaticResource StatusReviewBg}" CornerRadius="6" Padding="8,6" Margin="0,0,0,10">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource StatusReviewDot}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="3. EN PLANCHADO" FontWeight="Bold" FontSize="12" Foreground="{StaticResource StatusReviewText}"/>
                            </StackPanel>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding ColumnaEnPlanchado.Count}" FontWeight="ExtraBold" Foreground="{StaticResource StatusReviewText}" FontFamily="{StaticResource MonoFont}"/>
                        </Grid>
                    </Border>
                    <!-- Tarjetas de órdenes -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ColumnaEnPlanchado}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8" Padding="12">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <TextBlock Text="{Binding NumeroTicket}" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock HorizontalAlignment="Right" Text="{Binding Items.Count, StringFormat='{}{0} pzs'}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" FontSize="12"/>
                                            <TextBlock Text="{Binding FechaPromesaEntrega, StringFormat='Entrega: {0:dd/MM}'}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,8"/>

                                            <Button Style="{StaticResource SuccessButton}" Content="Listo para Retiro ✓" Command="{Binding DataContext.MarcarListoParaEntregaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="6,4" FontSize="11" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- COLUMNA 4: LISTO PARA ENTREGA / MOSTRADOR -->
            <Border Grid.Column="6" Background="#ECFDF5" BorderBrush="#A7F3D0" BorderThickness="1" CornerRadius="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Cabecera -->
                    <Border Grid.Row="0" Background="{StaticResource StatusDoneBg}" CornerRadius="6" Padding="8,6" Margin="0,0,0,10">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="{StaticResource StatusDoneDot}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="4. LISTO EN MOSTRADOR" FontWeight="Bold" FontSize="12" Foreground="{StaticResource StatusDoneText}"/>
                            </StackPanel>
                            <TextBlock HorizontalAlignment="Right" Text="{Binding ColumnaListoParaEntrega.Count}" FontWeight="ExtraBold" Foreground="{StaticResource StatusDoneText}" FontFamily="{StaticResource MonoFont}"/>
                        </Grid>
                    </Border>
                    <!-- Tarjetas de órdenes -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ColumnaListoParaEntrega}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ModernCard}" Margin="0,0,0,8" Padding="12">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,4">
                                                <TextBlock Text="{Binding NumeroTicket}" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock HorizontalAlignment="Right" Text="{Binding SaldoPendiente, StringFormat='Pend: RD${0:N0}'}" FontSize="11" FontWeight="Bold" Foreground="#DC2626"/>
                                            </Grid>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" FontSize="12"/>
                                            <TextBlock Text="{Binding ClienteTelefono}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,8"/>

                                            <Button Style="{StaticResource PrimaryButton}" Content="Entregar al Cliente ✓" Command="{Binding DataContext.EntregarAlClienteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="6,4" FontSize="11" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
