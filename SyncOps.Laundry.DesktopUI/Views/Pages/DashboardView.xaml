<UserControl x:Class="SyncOps.Laundry.DesktopUI.Views.Pages.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,10,0">
        <StackPanel Margin="0,0,0,20">
            <!-- HEADER DE BIENVENIDA Y ACCIONES RÁPIDAS -->
            <Grid Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Panel de Control General" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                    <TextBlock Text="Métricas operativas en tiempo real de lavandería, sastrería, autoservicio y finanzas" FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding IrANuevaOrdenCommand}" Margin="0,0,10,0" Padding="16,9">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconPlus}" Stroke="#FFFFFF" StrokeThickness="2" Width="12" Height="12" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Nueva Orden / Recepción" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource OutlineButton}" Command="{Binding CargarDashboardCommand}" Padding="14,9">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconRefresh}" Stroke="{StaticResource TextBodyBrush}" StrokeThickness="1.5" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Actualizar" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- FILA 1: 4 TARJETAS KPI MODERNAS (ESTILO SYNCOPS WEB) -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- KPI 1: Ventas Hoy -->
                <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="VENTAS HOY" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                            <!-- Icon Box Blue Subtle -->
                            <Border HorizontalAlignment="Right" Width="36" Height="36" CornerRadius="8" Background="{StaticResource BlueSubtleBrush}" BorderBrush="{StaticResource BlueBorderBrush}" BorderThickness="1">
                                <Path Data="{StaticResource IconCash}" Stroke="{StaticResource BlueVibrantBrush}" StrokeThickness="1.5" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding VentasHoy, StringFormat='RD${0:N2}'}" FontSize="23" FontWeight="ExtraBold" Foreground="{StaticResource TextMainBrush}" FontFamily="{StaticResource MonoFont}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Border Background="{StaticResource StatusDoneBg}" CornerRadius="12" Padding="6,2" Margin="0,0,6,0">
                                <TextBlock Text="✓ Al Día" FontSize="10" FontWeight="Bold" Foreground="{StaticResource StatusDoneText}"/>
                            </Border>
                            <TextBlock Text="Ingresos por caja y fichas" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- KPI 2: Prendas en Taller -->
                <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="PRENDAS EN TALLER" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                            <Border HorizontalAlignment="Right" Width="36" Height="36" CornerRadius="8" Background="{StaticResource IndigoSubtleBrush}" BorderBrush="{StaticResource IndigoBorderBrush}" BorderThickness="1">
                                <Path Data="{StaticResource IconScissors}" Stroke="{StaticResource IndigoPrimaryBrush}" StrokeThickness="1.5" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding PrendasEnTaller}" FontSize="23" FontWeight="ExtraBold" Foreground="{StaticResource TextMainBrush}" FontFamily="{StaticResource MonoFont}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Border Background="{StaticResource IndigoSubtleBrush}" CornerRadius="12" Padding="6,2" Margin="0,0,6,0">
                                <TextBlock Text="En Proceso" FontSize="10" FontWeight="Bold" Foreground="{StaticResource IndigoPrimaryBrush}"/>
                            </Border>
                            <TextBlock Text="Lavado y sastrería activa" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- KPI 3: Autoservicio & Torres -->
                <Border Grid.Column="4" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="AUTOSERVICIO" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                            <Border HorizontalAlignment="Right" Width="36" Height="36" CornerRadius="8" Background="{StaticResource StatusDoneBg}" BorderBrush="{StaticResource StatusDoneBorder}" BorderThickness="1">
                                <Path Data="{StaticResource IconWasher}" Stroke="{StaticResource StatusDoneDot}" StrokeThickness="1.5" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding MaquinasEnUso}" FontSize="23" FontWeight="ExtraBold" Foreground="{StaticResource BlueVibrantBrush}" FontFamily="{StaticResource MonoFont}"/>
                            <TextBlock Text=" en ciclo / " FontSize="13" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Bottom" Margin="4,0,4,3"/>
                            <TextBlock Text="{Binding MaquinasDisponibles}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource StatusDoneDot}" FontFamily="{StaticResource MonoFont}" VerticalAlignment="Bottom" Margin="0,0,0,3"/>
                            <TextBlock Text=" libres" FontSize="13" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Bottom" Margin="4,0,0,3"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Border Background="{StaticResource StatusDoneBg}" CornerRadius="12" Padding="6,2" Margin="0,0,6,0">
                                <TextBlock Text="Torres Monedas" FontSize="10" FontWeight="Bold" Foreground="{StaticResource StatusDoneText}"/>
                            </Border>
                            <TextBlock Text="Operación continua" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- KPI 4: Hoteles & Crédito -->
                <Border Grid.Column="6" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="CRÉDITO HOTELES" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                            <Border HorizontalAlignment="Right" Width="36" Height="36" CornerRadius="8" Background="{StaticResource StatusReviewBg}" BorderBrush="{StaticResource StatusReviewBorder}" BorderThickness="1">
                                <Path Data="{StaticResource IconClients}" Stroke="{StaticResource StatusReviewDot}" StrokeThickness="1.5" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding SaldoPendienteCobroHoteles, StringFormat='RD${0:N2}'}" FontSize="23" FontWeight="ExtraBold" Foreground="#B45309" FontFamily="{StaticResource MonoFont}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Border Background="{StaticResource StatusReviewBg}" CornerRadius="12" Padding="6,2" Margin="0,0,6,0">
                                <TextBlock Text="Por Cobrar" FontSize="10" FontWeight="Bold" Foreground="{StaticResource StatusReviewText}"/>
                            </Border>
                            <TextBlock Text="Cuentas corporativas" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- FILA 2: TABLAS PRINCIPALES (Últimas Órdenes y Alertas de Insumos) -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <!-- ÓRDENES RECIENTES -->
                <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Órdenes de Servicio Recientes" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                            </StackPanel>
                            <Button HorizontalAlignment="Right" Content="Ver Flujo en Taller →" Style="{StaticResource OutlineButton}" Command="{Binding IrATallerCommand}" Padding="10,5" FontSize="11.5"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding UltimasOrdenes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="130"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="110"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Ticket -->
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding NumeroTicket}" FontWeight="ExtraBold" Foreground="{StaticResource BluePrimaryBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock Text="{Binding FechaRecepcion, StringFormat='{}{0:dd/MM hh:mm tt}'}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>

                                            <!-- Cliente y Tipo -->
                                            <StackPanel Grid.Column="1" Margin="10,0">
                                                <TextBlock Text="{Binding ClienteNombre}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" FontSize="12.5"/>
                                                <TextBlock Text="{Binding ClienteTelefono}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>

                                            <!-- Estado Badge -->
                                            <Border Grid.Column="2" CornerRadius="12" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center" Background="{StaticResource StatusProgressBg}" BorderBrush="{StaticResource StatusProgressBorder}" BorderThickness="1">
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="5" Height="5" Fill="{StaticResource StatusProgressDot}" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Estado}" FontWeight="Bold" FontSize="10.5" Foreground="{StaticResource StatusProgressText}"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Total -->
                                            <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Total, StringFormat='RD${0:N2}'}" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" FontFamily="{StaticResource MonoFont}" FontSize="13"/>
                                                <TextBlock Text="{Binding SaldoPendiente, StringFormat='Pend: RD${0:N2}'}" FontSize="10.5" Foreground="#DC2626" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- INSUMOS CRÍTICOS -->
                <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <TextBlock Text="Alertas de Insumos" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                            <Button HorizontalAlignment="Right" Content="Inventario →" Style="{StaticResource OutlineButton}" Command="{Binding IrAInventarioCommand}" Padding="10,5" FontSize="11.5"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding InsumosCriticos}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource StatusDangerBg}" BorderBrush="{StaticResource StatusDangerBorder}" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding NombreInsumo}" FontWeight="Bold" FontSize="12" Foreground="{StaticResource StatusDangerText}"/>
                                                <TextBlock Text="{Binding ProveedorPrincipal, StringFormat='Prov: {0}'}" FontSize="10.5" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding StockActual, StringFormat='{}{0:N0} disp.'}" FontWeight="ExtraBold" Foreground="{StaticResource StatusDangerDot}" FontFamily="{StaticResource MonoFont}"/>
                                                <TextBlock Text="{Binding StockMinimoAlerta, StringFormat='Mín: {0:N0}'}" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
