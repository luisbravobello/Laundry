<UserControl x:Class="SyncOps.Laundry.DesktopUI.Views.Pages.InventarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="1.1*"/>
        </Grid.ColumnDefinitions>

        <!-- COLUMNA IZQUIERDA: LISTADO DE INSUMOS -->
        <Border Grid.Column="0" Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Inventario de Insumos &amp; Suministros" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}"/>
                        <TextBlock Text="Detergentes industriales, suavizantes, fundas plásticas, ganchos e hilos de sastrería" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Style="{StaticResource OutlineButton}" Command="{Binding CargarInsumosCommand}" Content="⟳ Refrescar" Padding="10,5"/>
                </Grid>

                <!-- TABLA DE INSUMOS -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Insumos}" SelectedItem="{Binding InsumoSeleccionado}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0" Background="Transparent" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderBrush}" RowHeight="42">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="100"/>
                        <DataGridTextColumn Header="Nombre del Insumo" Binding="{Binding NombreInsumo}" Width="*"/>
                        <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="120"/>
                        <DataGridTextColumn Header="Stock Actual" Binding="{Binding StockActual, StringFormat='{}{0:N0}'}" Width="100"/>
                        <DataGridTextColumn Header="Stock Mínimo" Binding="{Binding StockMinimoAlerta, StringFormat='{}{0:N0}'}" Width="100"/>
                        <DataGridTextColumn Header="Costo Unit." Binding="{Binding CostoUnitarioCompra, StringFormat='RD${0:N2}'}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- COLUMNA DERECHA: REABASTECIMIENTO & NUEVO INSUMO -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- REABASTECER INSUMO SELECCIONADO -->
                <Border Style="{StaticResource ModernCard}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Reabastecer / Entrada de Stock" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding InsumoSeleccionado.NombreInsumo, FallbackValue='Selecciona un insumo de la tabla'}" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Cantidad a Agregar:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding CantidadAjuste, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource MonoFont}"/>
                            </StackPanel>
                            <Button Grid.Column="2" Style="{StaticResource SuccessButton}" Content="Entrada +" Command="{Binding ReabastecerInsumoCommand}" CommandParameter="{Binding InsumoSeleccionado}" VerticalAlignment="Bottom" Height="36"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- FORMULARIO NUEVO INSUMO -->
                <Border Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <TextBlock Text="Registrar Nuevo Insumo" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BluePrimaryBrush}" Margin="0,0,0,12"/>

                        <TextBlock Text="Nombre del Insumo:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevoNombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <TextBlock Text="Unidad de Medida (Galones, Rollos, Cajas):" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevaUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Stock Inicial:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevoStockInicial, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource MonoFont}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Stock Mínimo:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevoStockMinimo, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource MonoFont}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Costo Unitario de Compra (RD$):" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevoCosto, UpdateSourceTrigger=PropertyChanged}" FontFamily="{StaticResource MonoFont}" Margin="0,0,0,10"/>

                        <TextBlock Text="Proveedor Principal:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,4"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding NuevoProveedor, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

                        <Button Style="{StaticResource PrimaryButton}" Command="{Binding RegistrarNuevoInsumoCommand}" Content="Guardar Insumo" Height="38"/>
                        <TextBlock Text="{Binding MensajeEstado}" Foreground="{StaticResource BluePrimaryBrush}" FontWeight="SemiBold" FontSize="11" HorizontalAlignment="Center" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
